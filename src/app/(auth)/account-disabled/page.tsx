import { AccountDisabledView } from "@/components/auth/account-disabled-view";

export const dynamic = "force-dynamic";

export default function AccountDisabledRoute() {
  return <AccountDisabledView />;
}
